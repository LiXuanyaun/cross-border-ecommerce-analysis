# Changelog

## 4.0.0 - 2026-07-28

### 多业务数据闭环

- 新增 AdventureWorksDW 无表头 `|` 分隔订单适配器，保留订单号和订单行号并关联产品、品类、客户、地区、币种和汇率。
- 新增广告、归因、退款、物流、轨迹及三个维表的独立 SQLite 模型、导入批次、文件哈希和业务键幂等保护。
- 新增广告、退款和物流注册指标、版本化异常规则、证据、限制和行动建议，以及对应 FastAPI API 与 React 专题页面。
- Excel、DOCX、Markdown、manifest 和受控 Agent 上下文复用同一多业务指标和证据；Agent 不开放任意 SQL。

### 数据治理与验证

- 校验归因、点击/转化、退款上限、物流关联与严格时间顺序、跨境清关事件和 USD ROAS 口径。
- 所有 AdventureWorks 扩展输出明确标记为模拟数据；德国搜索广告、Clothing 尺码退货、GlobalPost 欧洲线路延误三个场景由规则识别。

## 3.1.1 - 2026-07-28

### 架构

- 拆分 FastAPI 路由模块，保留所有现有 `/api/v1` 路径。
- 提取 DatasetService、AnalysisQueryService、WorkItemService、AgentContextBuilder、OverviewPresenter 和 TopicPresenter。
- 按 feature 目录整理 React 页面实现，并保留 `pages/*` 兼容导出。
- 新增架构、数据模型路线、导入和维护文档。

### 验证

- 后端测试、前端测试、前端构建、React E2E 和 10 万行 SQLite 性能基准通过。

## 3.1.0 - 2026-07-28

### 基线

- 建立架构优化前的已验证能力基线，统一 Python、API、前端和报告 manifest 的产品版本号。
- 保留现有 API、分析口径、SQLite 数据层、React 主界面和 Streamlit 旧版调试界面行为不变。

### 验证

- 后端测试、前端测试、前端构建和 10 万行 SQLite 性能基准通过。

## 2.5.0 - 2026-07-19

### 新增

- 新增稳定的指标、快照、异常、诊断、建议、证据和 Insight 领域对象及 SQLite 持久化表。
- 新增 Metrics Catalog v1、九条版本化异常规则、驱动贡献诊断和诊断门槛建议。
- 新增 A-D 数据可信度、字段健康、分析能力地图、不可回答结论和数据提升计划。
- Dashboard 新增风险中心、洞察中心和数据健康中心；Excel、DOCX 和 manifest 统一消费 Phase 2 对象。
- 新增受控 EvidenceBundle 重放、结果摘要哈希、稳定 scope ID 和重复运行幂等保护。
- 新增 MarketOpportunity、ProductOpportunity 和 ActionItem 稳定对象，统一进入 SQLite、Dashboard、Excel、DOCX 和 manifest。
- 新增市场增长矩阵、产品机会三视角矩阵、可筛选行动清单、详情与 CSV 下载。
- 自动报告统一为《跨境电商经营分析与行动报告》，支持摘要版/完整版、整体/市场/品类范围和可选行动明细。
- 商品机会候选使用跨两期合计至少 3 单的公开门槛，门槛以下只计入样本不足汇总，避免生成低证据行动项。

### 兼容性

- 保留现有七个分析页面、`AnalysisService.run(context, filters=...)` 和 pandas 对账后端。
- SQL 继续作为正式事实来源；低样本、C/D 级质量、缺字段和不完整周期显式抑制，不生成伪结论。

## 2.2.1 - 2026-07-19

### 工程化

- AutoClean 依赖固定到 GitHub `v6.5.0` Tag，移除对相邻 `../AutoClean-v1` 目录的依赖。
- 增加 SQLite 表结构、数据集版本、事务边界和命名查询目录文档。

### 修复

- 全部 Dashboard 图表统一使用响应式 Plotly 布局和渲染入口，修复标题、图例、坐标轴、色条及中文标签裁切。
- 时间轴根据周期数量自动旋转并降低刻度密度；分类轴自动换行，水平图和热力图按内容动态增加高度。
- 深色系统偏好下继续使用明确的浅色经营控制台字体和图表配色，不再由 Streamlit 主题覆盖。
- 报告中的 Matplotlib 图表统一中文字体回退、约束布局和安全保存边距。

### 验证

- 增加长中文标签、36个月时间轴、动态图例、热力图全品类和报告图片画布测试。
- Chrome 与 Edge 验收扩展至 1440、1280、1024 和 393 像素视口。

## 2.2.0 - 2026-07-19

### 新增

- 电商契约升级为 `cross_border_orders_v2`，新增可选国家和商品名称字段；市场分析按国家优先、区域回退选择统一粒度。
- 新增市场×动态品类热力图、市场策略矩阵、客户市场/品类构成和标准化证据字段。
- 商品清单支持动态分类、完整分页、搜索与列筛选、当前预览下载，并可在同页打开商品详情。
- 新增参数化商品摘要、月度表现、市场构成和客户构成查询，作为未来 Agent 的受控数据入口。
- Excel 与 manifest 增加市场品类、客户构成和商品详情查询来源。

### 调整

- 页面导航移动到侧栏顶部；全局只保留分析周期，数据上传、币种、汇率和字段映射收进数据口径区域。
- 移除全局区域与品类按钮；页面局部选择不再改变固定模型、证据、建议和完整报告。
- 删除商品清单前 500 项截断，国家或商品详情可选字段不足时仅降级对应展示区域。

## 2.1.0 - 2026-07-19

### 新增

- 基于 AutoClean 6.5 的版本化 SQLite 分析数据层。
- 新增经营总览、月度销售、销售贡献、市场、商品、亏损商品、客户 RFM 基础和退货分析命名查询。
- 六个业务模块使用 SQL 聚合，Python 保留业务阈值、分群、证据和建议。
- manifest 增加数据库模式、数据集编号和查询耗时。
- Dashboard 数据准备视图显示本地数据层状态。

### 兼容性

- 原始文件仍保持只读；重复订单继续作为订单粒度冲突，不自动去重。
- 保留显式 pandas 后端用于兼容和结果对账，Dashboard 与 CLI 默认使用 SQL。
