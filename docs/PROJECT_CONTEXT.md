# Project Context

## 1. Project Mission

项目为什么存在？CrossBorder AI Analytics 将跨境电商订单数据转化为可追溯、可复算、可执行的经营判断，而不只是展示图表或生成聊天回答。

解决什么问题？统一数据质量、指标口径、周期比较、异常、证据、建议、任务和报告，帮助运营人员完成经营复盘与问题处置。

目标用户是谁？跨境电商运营负责人、经营分析人员和管理者；当前定位是本地单用户、试点级商业 MVP。

---

## 2. Product Goal

当前产品目标：在 `4.0.0` 架构基础上，以可审计的统一数据身份承载 `demo-all` 与 AdventureWorks 的订单分析和多业务分析，并把真实来源、演示推算、质量限制和证据边界持续传递到全部产品表面。

核心价值：原始数据只读；结论有正式口径和证据；字段、质量或周期不足时明确降级；建议可以进入任务和复盘。

当前阶段：React/FastAPI 和 SQLite 主链路已可用，正在收敛性能、容量、前端维护性、Agent 真实性和真实用户验证。

---

## 3. Current Status

已经完成：
- CSV/XLSX 预检与导入、AdventureWorks 适配器、版本化 SQLite、指标/异常/诊断/建议/证据链、React 主产品、报告与任务闭环。
- 广告、退款和物流已使用独立业务事实表、导入批次/哈希和注册规则；API、专题页面、报告与 Agent 共用指标和证据。
- FastAPI 已拆分路由、服务、Presenter 和 Agent 上下文；Streamlit 运行时和兼容入口已移除。
- Agent 已有注册工具、计划、SSE 工具事件和确定性降级；私有模式支持 provider 与本地状态持久化。
- 已发布统一数据集 `unified-636541dc03cecb5d`：94,898 条规范化订单事实保留逐行来源键、版本和合并审计，目标币种为 CNY。
- 广告、退款和物流保持独立业务粒度，但全部归属统一 `dataset_id`；普通目录和多业务目录都只暴露统一入口。
- 总览、普通专题、多业务专题、报告、任务和 Agent 共用统一身份；唯一数据集在前端自动选中。
- 私有模式现在会在目录中同时暴露统一数据集和已完成的 Web 导入数据集，导入后可在看板顶部切换到 `ecommerce_sales_34500`，并保持各自的周期和质量状态。
- Web 数据支持跨批次“追加到已有数据集”：导入确认时可选择目标 Web 数据集，系统按同构订单合同和自然键校验后生成带 `parent_dataset_id` 的版本化子数据集；旧版本保持只读，重复来源行幂等跳过，跨批次重复 `order_id` 整批阻断，成功后看板自动切换到新版本。
- 客户专题区分正常、异常和数据不足，多业务首屏使用业务语言披露“演示推算数据”。
- 启动脚本使用工作区、模式和代码指纹判断复用或重启，只处理已验证的同工作区旧进程，并在 private 模式打开浏览器前检查统一数据可用。
- 三个旧来源/重复派生注册已停用，恢复窗口截至 2026-08-07；物理清理受时间和引用双重门禁保护。

正在进行：
- 等待七天回滚窗口自然结束；到期后重新预览引用，仅物理清理获批且无引用的派生数据。该等待不是实现阻断。

未来计划：
- 根据真实接入、任务采纳、持续使用和付费证据，再决定是否接入成本、库存、活动、渠道、店铺并扩大 Agent 自主性。

---

## 4. Technical Architecture

技术栈：Python 3.10+、AutoClean 6.6、pandas、SQLite、FastAPI、React 18、TypeScript、TanStack Query、ECharts、TailwindCSS、pytest、Vitest、Playwright 和 Docker。

模块关系：`frontend` -> `crossborder_api` routes -> services/presenters -> `crossborder_analytics` -> SQLite/ArtifactStore；`AnalyticsRuntime` 只作为兼容 facade。

数据流：CSV/XLSX 或 AdventureWorksDW -> 预检和标准化 -> 版本化订单/多业务事实数据集 -> 命名 SQL/注册指标 -> 异常/诊断/建议/证据 -> 页面、任务、报告和受控 Agent。

关键组件：`crossborder_analytics/` 负责分析核心、领域对象、存储和报告；`crossborder_api/` 负责 API、应用服务、任务和 Agent；`frontend/src/features/` 负责主页面；`data/ecommerce_sales_34500.csv` 是默认样例，`data/AdventureWorksDW-data/` 是只读多业务订单源。

---

## 5. Important Decisions

Decision: React + FastAPI 是唯一主产品；历史口径通过领域测试、API 契约和报告回归验证，不再保留第二套 UI。
Reason: 双 UI 链路增加依赖和维护成本，并允许数据身份与交互行为分叉。
Date: 2026-07-31

Decision: 原始数据只读；SQL 和注册指标是正式事实层；不开放任意 SQL。
Reason: 保证事实、口径、权限和证据血缘可审计。
Date: v3.1

Decision: `dataset_id`、`scope_id`、周期、币种和质量状态贯穿页面、报告、任务和 Agent；Agent 先使用受控工具与透明降级。
Reason: 防止混算并保证结论可复算；先验证证据边界，再扩大模型自主性。
Date: 2026-07-28

Decision: AdventureWorks 订单保持兼容订单事实；广告、退款和物流使用独立事实表，并由导入批次、文件哈希、自然业务键和外键保证血缘、隔离与幂等。
Reason: 不混淆业务粒度，且可以在不破坏原有订单分析和 API 契约的前提下接入扩展数据。
Date: 2026-07-28

Decision: AdventureWorks 扩展数据在当前版本是模拟数据，所有页面、报告、API 和 Agent 上下文必须持续披露来源和限制。
Reason: 保护经营判断，避免将演示验证场景误报为真实运营事实或因果。
Date: 2026-07-28

Decision: 统一数据集先经过只读预演；订单行键、币种、金额或日期语义存在未确认项时，禁止物化、切换入口或清理旧派生数据。
Reason: 当前两份来源在粒度、币种和期间上均不等价，直接拼接会制造不可审计的总额与去重假象。
Date: 2026-07-31

Decision: 统一数据 v1 将 `demo-all` 的每个订单映射为确定性行号 `1`，AdventureWorks 保留原订单行；金额统一为折后商品金额，运费单列，不推断税费，USD 使用版本化演示汇率 `7.20` 转为 CNY，日期保持来源本地自然日。
Reason: 这些规则让两份来源在不伪造业务语义的前提下进入同一可审计事实层，并允许逐行回溯和精确复算。
Date: 2026-07-31

Decision: 普通订单事实与广告、退款、物流事实共享统一 `dataset_id`，但不合并业务粒度；旧注册只做可恢复停用，物理清理必须同时通过七天时间闸门和引用检查。
Reason: 用户只选择一次数据即可运行完整产品，同时避免把归因、退款或物流事件错误解释为订单行。
Date: 2026-07-31

Decision: 客户专题将“正常”“异常”“数据不足”作为正式后端状态；正常态可展示规模、分群贡献、证据和监测动作，但不得制造异常或因果。
Reason: 正常经营状态也是有效结果，同时必须与字段或样本不足严格区分。
Date: 2026-07-31

Decision: 数据集能力使用版本化 `1.0.0` 契约，从 SQLite 事实计算分段可用时期、推荐时期和正式数据状态；订单推荐最近完整月，多业务事实推荐最新连续区间内近 12 个月，前端以 `AUTO/USER` 标记范围来源。
Reason: 数据集和专题切换不能继承历史固定日期，用户明确选择的范围也不能被系统静默覆盖；空事实必须与真实零值分离。
Date: 2026-07-31

Decision: 普通专题和多业务专题由后端展示契约注册图表类型、维度、序列、格式、行数据以及表格列和分页；前端只负责布局、交互和格式渲染，空行不挂载图表。
Reason: KPI、趋势、排名、明细、报告和 Agent 必须共享同一事实范围，字段含义和数据选择不能由页面组件重复推断。
Date: 2026-07-31

Decision: Web 导入的后续数据使用显式纵向追加并生成版本化子数据集，不原地覆盖父数据集；追加只允许 READY Web 订单数据集，校验粒度、金额语义、目标币种和跨批次 `order_id` 冲突，成功后刷新目录与看板。
Reason: 保留报告、任务和 scope 对旧版本的可审计引用，避免原地 UPDATE 造成历史结果漂移，同时满足按月/店铺/市场拆分文件的持续接入。
Date: 2026-07-31

---

## 6. AI Development Rules

修改代码时：遵循根目录 `AGENTS.md`；复杂任务必须先更新 `PLAN.md`，再进入实现。

必须：先定位后读取；保持真实数据、统一 scope、完整可比周期和 API 契约；运行与风险匹配的测试并说明未运行项。

不要：不用 Mock、随机数、Placeholder 或前端猜测制造业务结论；不覆盖用户改动；不把旧评分、旧 PRD 或历史测试结果写成当前状态。

测试要求：后端 pytest；前端 Vitest + build；布局用浏览器回归；数据层变更运行 10 万行基准。

文档要求：稳定决策更新本文件；每日进展写入 `docs/founder-log/`；安装和命令写入 README。

---

## 7. Known Risks

当前风险：统一数据 v1 使用固定演示汇率和明确的演示口径，不应被误当成真实财务折算；`data/AdventureWorksDW-data/` 仍是只读来源，永不进入自动物理清理范围。旧注册在 2026-08-07 前必须保持可恢复。

技术风险：前端 feature 文件仍偏大；SQLite 快照/缓存会增长；性能受环境和数据形态影响；导入暂存与性能遥测仍需继续收敛。

产品风险：Agent 仍是受控工具链加可选模型；当前广告、退款和物流为模拟扩展数据，且仍缺少成本、库存、活动和真实多店铺数据，不能完成经营因果归因。

商业风险：持续使用、任务采纳、实际收益、接入成本和付费意愿尚未由真实试点证明。
