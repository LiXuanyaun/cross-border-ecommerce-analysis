# AdventureWorks v4 fixture

This tiny fixture mirrors the headerless pipe-delimited AdventureWorks order sources and the eight generated business files. It contains one order line, one attributed campaign, one return, one shipment and three ordered tracking events. All extension rows are explicitly marked `synthetic_extension`.
